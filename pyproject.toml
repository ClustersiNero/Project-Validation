[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "validation"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
validation-demo = "validation.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
